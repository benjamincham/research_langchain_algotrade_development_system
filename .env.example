# LLM Provider Credentials
# Copy this file to .env and fill in your actual API keys
# DO NOT commit .env to git (it's in .gitignore)

# At least ONE provider must be configured for the system to work
# Multiple providers enable automatic failover

# ============================================================================
# OpenAI (Recommended)
# ============================================================================
# Get your API key from: https://platform.openai.com/api-keys
# Model used: gpt-4o-mini
# Cost: $0.15-0.60 per 1M tokens
OPENAI_API_KEY=sk-...

# ============================================================================
# Anthropic Claude (High Quality)
# ============================================================================
# Get your API key from: https://console.anthropic.com/
# Model used: claude-3-5-haiku-20241022
# Cost: $0.80-4.00 per 1M tokens
ANTHROPIC_API_KEY=sk-ant-...

# ============================================================================
# Google Gemini (Free Tier Available)
# ============================================================================
# Get your API key from: https://aistudio.google.com/app/apikey
# Model used: gemini-2.0-flash-exp
# Cost: Free tier available
GOOGLE_API_KEY=AIza...

# ============================================================================
# Azure OpenAI (Enterprise)
# ============================================================================
# Get credentials from Azure Portal
# Note: Requires Azure OpenAI resource setup
AZURE_OPENAI_API_KEY=...
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=your-deployment-name

# ============================================================================
# Groq (Fast & Cheap)
# ============================================================================
# Get your API key from: https://console.groq.com/
# Model used: llama-3.3-70b-versatile
# Cost: $0.59-0.79 per 1M tokens
GROQ_API_KEY=gsk_...

# ============================================================================
# Application Settings
# ============================================================================
LOG_LEVEL=INFO
ENVIRONMENT=development

# ============================================================================
# Recommended Configurations
# ============================================================================

# Development (Free/Cheap):
# - GOOGLE_API_KEY (free tier)
# - GROQ_API_KEY (very cheap)

# Production (Reliability):
# - OPENAI_API_KEY (primary)
# - ANTHROPIC_API_KEY (fallback)
# - GOOGLE_API_KEY (fallback)
# - GROQ_API_KEY (fallback)

# Production (Cost-Optimized):
# - GROQ_API_KEY (primary, cheapest)
# - GOOGLE_API_KEY (fallback, free)
# - OPENAI_API_KEY (emergency fallback)
